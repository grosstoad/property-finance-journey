/**
 * Default values and configuration for the application
 * These can be easily modified for different scenarios
 */

// Default percentage of property price to use for savings
export const DEFAULT_SAVINGS_PERCENTAGE = 25; // 25% of property price

// Default upfront costs (as per PRD)
export const DEFAULT_UPFRONT_COSTS = 3000; // $3,000

// Debounce time for input fields (in ms)
export const INPUT_DEBOUNCE_TIME = 500; // 500ms

// Maximum LVR for property loans
export const DEFAULT_MAX_LVR = 80; // 80% LVR

// Loan and deposit calculation defaults
export const DEFAULT_MIN_UPFRONT_COSTS = 1500;
export const DEFAULT_UPFRONT_COSTS_PERCENTAGE = 0.001; // 0.1% of property price 